# ESP32 firmware

`sky_rendererv3/` is a working copy of the supplied ESP32 sketch. Keep hardware-specific work here; the original remains in `originals/firmware/` for comparison.

Before changing PCB or CAD scope, record the exact ESP32, display, GPS receiver, power architecture, and measured component dimensions in `hardware/`.
